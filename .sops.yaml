# One personal age key encrypts every secret. The private half lives in the
# password manager and at ~/.config/sops/age/keys.txt on each machine.
keys:
  - &amr age14f9ldtps5a8pv472rcczfa8qmy68j6ft7krvm6cq96vu6suxc5xqjr0mq8
creation_rules:
  - path_regex: ^secrets/.*\.yaml$
    key_groups:
      - age:
          - *amr
